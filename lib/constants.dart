import 'package:flutter/material.dart';

enum Locale {
  local,
  state,
  national,
  global,
}

const Icon kLocalIcon = Icon(Icons.house);
const Icon kStateIcon = Icon(Icons.assured_workload);
const Icon kNationalIcon = Icon(Icons.flag);
const Icon kGlobalIcon = Icon(Icons.blur_circular);

bool shoWsecreTscreen = true;
