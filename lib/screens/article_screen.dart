import 'package:flutter/material.dart';

class ArticleScreen extends StatelessWidget {
  const ArticleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    //TODO build out article screen
    return Scaffold(
      appBar: AppBar(
        title: Text('data'),
      ),
      body: Text('data'),
    );
  }
}
